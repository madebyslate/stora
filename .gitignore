node_modules/
.pnpm-store/

# build output
dist/
.astro/
.next/
.turbo/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# media / uploads (produkcja trzyma je w docker volume)
apps/cms/media/

# Zakodowane wideo tła JEST w repo. Master leży w _inbox/ (gitignore), więc gdyby
# wynik `scripts/encode-video.sh` tu nie trafił, `docker build` zbudowałby hero
# bez wideo — i nie widać tego lokalnie, bo lokalnie plik jest. Patrz PLAYBOOK P-014.

# testy
test-results/
playwright-report/
blob-report/
.lighthouseci/
.lighthouseci-desktop/

# lokalne
.DS_Store
**/.DS_Store
*.local

# skrzynka na materiały źródłowe (patrz _inbox/README.md)
_inbox/*
!_inbox/README.md
